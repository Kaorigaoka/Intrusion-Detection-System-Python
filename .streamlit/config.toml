# Streamlit theme — gives the dashboard a consistent dark "SOC" look,
# locally and when deployed to Streamlit Community Cloud.
[theme]
base = "dark"
primaryColor = "#38bdf8"
backgroundColor = "#0b1220"
secondaryBackgroundColor = "#121a2b"
textColor = "#e2e8f0"

[server]
headless = true
